<?php
    defined("__READMEFILE") or die("Invalid access");
    
 /**   switch($subact)
    {
        case "setup": // Quick Setup guide
        case "problems": // If you have problems...
        case "ccs": // Custom Command system
        case "adv": // advanced features like config.ini hacks
        case "script": // scripting system
        case "clos": // command-line options
        case "codes": // 4-letter game codes
        case "links": // useful StealthBot-related links
        case "flags": // Default bot flag reference
        case "donate":
            echoln("<h2>Page not ready</h2>");
            echoln("Try again later. Sorry!");
            break;
            
        default:
            echoln("<h2>Invalid action</h2>");
            echoln("Please press the Back button in your browser and try your link again.");
    }**/
    
    $PAGETITLES = array(
    
    );
    // default bot flags
    // useful links - ie battle.net/info/channels.shtml
    
    $PAGES = array(
    
    );
?>